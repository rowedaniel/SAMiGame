#include "../stdafx.h"
#include "MainMenu.h"
/*
MainMenu::MenuResult MainMenu::show(sf::RenderWindow & window)
{

	MenuItem playButton;
	playButton.rect.left = 50;
	playButton.rect.width = 160;
	playButton.rect.top = 47;
	playButton.rect.height = 72;
	playButton.action = Play; // Play

	MenuItem exitButton;
	exitButton.rect.left = 63;
	exitButton.rect.width = 315;
	exitButton.rect.top = 283;
	exitButton.rect.height = 70;
	exitButton.action = Exit; // Exit

	_menuItems.push_back(playButton);
	_menuItems.push_back(exitButton);
	
	// TODO: is this ugly?
	return static_cast<MenuResult>((runMenu(window)).action);
}*/
